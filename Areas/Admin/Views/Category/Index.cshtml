@model IEnumerable<Category>

@{
    ViewData["Title"] = "Index";
}

<br />
<br />

<div class="row">
    <div class="col-3">
        <h1>Category List</h1>
    </div>

    <div class="col-3">
        <p>
            <a asp-action="Create"
               class="btn btn-warning text-white font-weight-bold ml-1"
               style="line-height: 35px; font-size: 20px">
                + Create New Category
            </a>
        </p>
    </div>
</div>
<br />
<br />
@if (Model.Count() > 0)
{
    <table class="table table-striped border">
        <thead>
            <tr class="table-secondary">
                <th>
                    @Html.DisplayNameFor(model => model.Id)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td class="font-italic">
                        @Html.DisplayFor(modelItem => item.Id)
                    </td>
                    <td>
                        <div class="font-italic">@Html.DisplayFor(modelItem => item.Name)</div>

                        <partial name="_EditDetailsDeleteButtons" model="item.Id" />
                    </td>
                    
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p class="font-weight-bold">No category exists...</p>
}
